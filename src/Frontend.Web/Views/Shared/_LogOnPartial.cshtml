@using ApolloDb

@{
    var userSession = new SessionUser();
}

@if(userSession.IsLoggedIn) {
    <text>Hallo <strong>@userSession.Kandidat.GetVollerNameKY()</strong>!
    [ @Html.ActionLink("Abmelden", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Anmelden", "Login", "Account") ]
}
